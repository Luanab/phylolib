package data.tree;

import data.IReader;
import data.IWriter;

/**
 * Responsible for parsing and formatting {@link Tree phylogenetic trees} from and to Strings.
 */
public abstract class TreeProcessor implements IReader<Tree>, IWriter<Tree> {

}
