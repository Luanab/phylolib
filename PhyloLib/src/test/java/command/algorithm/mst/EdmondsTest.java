package command.algorithm.mst;

public class EdmondsTest {
/*
	@DataProvider
	public Object[][] data() {
		return new Object[][] {
				{ new Double[][] {
						{ 0.0, 1.0 },
						{ 1.0, 0.0 } },
						new Edge(0, 1, 1) }
		};
	}

	@Test(dataProvider = "data")
	public void process_Valid_Success(Double[][] values, Edge... edges) {
		Matrix matrix = new Matrix(false, null, values);
		List<Edge> expected = Arrays.asList(edges);

		Tree tree = new GrapeTree().process(matrix);

		assertEquals(tree.edges().size(), expected.size());
		assertTrue(tree.edges().stream()
				.allMatch(i -> expected.stream()
						.anyMatch(j -> i.from() == j.from() && i.to() == j.to() && i.distance() == j.distance())));
	}
*/
}
